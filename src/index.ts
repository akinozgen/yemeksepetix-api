import * as CityProvider from './providers/cityprovider';
import * as MenuProvider from './providers/menuprovider';
import * as RegionProvider from './providers/regionprovider';
import * as RestaurantProvider from './providers/restaurantprovider';

export default {
  CityProvider,
  MenuProvider,
  RegionProvider,
  RestaurantProvider,
};
